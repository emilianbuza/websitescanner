{
  "name": "website-scanner-ultimate",
  "version": "2.2.2",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "start:prod": "NODE_ENV=production node server.js",
    "dev": "node --watch server.js"
    /* WICHTIG: KEIN postinstall hier, Render führt bereits
       `npm install && npx playwright install chromium` aus */
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "playwright": "^1.46.0"
  },
  "engines": {
    "node": ">=18"
  }
}
